/// <reference types="vite/client" />

// Injected by vite.config.ts define from package.json. Declared here because
// tsconfig only includes vite/client types.
declare const __APP_VERSION__: string;
